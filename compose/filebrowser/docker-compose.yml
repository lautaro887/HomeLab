services:
  filebrowser:
    image: filebrowser/filebrowser:latest
    container_name: filebrowser
    restart: unless-stopped
    ports:
      - "8081:80"
    volumes:
      - ./data:/data/Filebrowser
      - ./database:./database
    environment:
      - PUID=1000
      - PGID=1000
